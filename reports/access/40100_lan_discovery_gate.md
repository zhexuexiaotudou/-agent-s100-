# 40100 LAN Discovery Gate

Implemented stable random device/installation identity, non-secret endpoint registry, Avahi `_http._tcp:80`, LAN/IP access model, `/healthz`, doctor, SVG QR generation and printable access card. LAN backend remains loopback.

Offline result: code and Linux install rendering pass. `digua.local`, Avahi conflict fallback, actual IP, port 80 and phone access are pending S100P execution.
