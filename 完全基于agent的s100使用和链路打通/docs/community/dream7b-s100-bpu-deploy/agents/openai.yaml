interface:
  display_name: "Dream7B S100 BPU Deploy"
  short_description: "Deploy and benchmark Dream7B on S100 BPU."
  default_prompt: "Use $dream7b-s100-bpu-deploy to validate Dream7B on S100P and prepare the deployment story."
policy:
  allow_implicit_invocation: true

