# Baseline Progress: Service Hardening Plan

Date: 2026-05-27

## Status

| Item | Status | Evidence |
| --- | --- | --- |
| B-010 hardening dry-run plan | verified path | `service_hardening_plan_probe` generates reviewable disable/firewall commands without changing services. |
| B-010 OpenClaw trigger | verified path | `s100p_run_probe` triggered `service_hardening_plan_probe` and returned the suggested decisions. |
| B-010 actual hardening | pending decision | Operator must decide whether to disable or firewall NFS/RPC, x11vnc, and iiod. |

## Implementation

Hardening plans are generated by:

```text
scripts/probes/service_hardening_plan_probe.sh
```

The allowlist runner exposes it as:

```bash
scripts/run_allowlisted_tool.sh service_hardening_plan_probe /root/.openclaw/workspace/logs/probes
```

OpenClaw exposes the same read-only path through:

```json
{"tool_id":"service_hardening_plan_probe"}
```

## Current Verdict

This moves B-010 from audit-only to a reviewable operator decision plan. It does
not disable services or change firewall rules.

## Runner Evidence

Runner report:

```text
/root/.openclaw/workspace/logs/probes/service_hardening_plan_20260527-060031.md
```

Observed decisions:

```text
OpenClaw Gateway: loopback, keep-loopback
SSH: present, keep-trusted-management
NFS/RPC server stack: present, disable-if-client-only
x11vnc: present, disable-if-unused
iiod: present, keep-or-firewall
```

Dry-run commands were printed for review, but not executed.

## OpenClaw Plugin Evidence

The OpenClaw agent used the real `s100p_run_probe` tool:

```text
runId: 154148d8-0224-45ae-95db-d4cf7e06a841
status: ok
tool_id: service_hardening_plan_probe
report: /root/.openclaw/workspace/logs/probes/service_hardening_plan_20260527-060121.md
NFS/RPC: disable-if-client-only
x11vnc: disable-if-unused
iiod: keep-or-firewall
```

## Remaining Gap

B-010 still needs an operator decision before any service or firewall changes:

```text
1. Confirm S100P is not exporting NFS shares.
2. Confirm x11vnc is not required for RDK Studio desktop access.
3. Confirm whether IIO tooling is needed; otherwise firewall or disable iiod.
4. After changes, rerun security_audit_probe and service_policy_probe.
```
