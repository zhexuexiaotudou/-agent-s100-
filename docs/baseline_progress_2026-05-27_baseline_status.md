# Baseline Progress: Baseline Status Roll-Up

Date: 2026-05-27

## Status

| Item | Status | Evidence |
| --- | --- | --- |
| Progress roll-up probe | verified path | `baseline_status_probe` summarizes the current board and workspace evidence into one Markdown report. |
| OpenClaw trigger | verified path | `s100p_run_probe` triggered `baseline_status_probe` and returned the key roll-up fields. |
| NAS-backed roll-up | pending | `/mnt/nas/openclaw` is not mounted yet. |

## Implementation

Baseline status reports are generated by:

```text
scripts/probes/baseline_status_probe.sh
```

The allowlist runner exposes it as:

```bash
scripts/run_allowlisted_tool.sh baseline_status_probe \
  /root/.openclaw/workspace \
  /root/.openclaw/workspace/reports/baseline-status
```

OpenClaw exposes the same read-only path through:

```json
{"tool_id":"baseline_status_probe"}
```

## Current Verdict

This moves progress reporting from scattered per-task files to a repeatable
status dashboard. It does not mark the baselines complete; it makes the
remaining blockers and next actions explicit.

## Runner Evidence

Runner report after syncing all progress docs:

```text
/root/.openclaw/workspace/reports/baseline-status/baseline_status_20260527-054653.md
```

Observed fields:

```text
Allowlisted tool count: 15
Progress docs: 15
NAS workspace: not_mounted
```

## OpenClaw Plugin Evidence

The OpenClaw agent used the real `s100p_run_probe` tool:

```text
runId: a0c43c05-a929-4a2a-9a94-2a3305139a52
status: ok
tool_id: baseline_status_probe
report: /root/.openclaw/workspace/reports/baseline-status/baseline_status_20260527-054753.md
Allowlisted tool count: 15
Progress docs: 15
NAS workspace status: not_mounted
```

After adding `nas_discovery_probe`, the roll-up was regenerated:

```text
report: /root/.openclaw/workspace/reports/baseline-status/baseline_status_20260527-055433.md
Allowlisted tool count: 16
Progress docs: 16
NAS workspace: not_mounted
```

After adding `service_hardening_plan_probe`, the roll-up was regenerated:

```text
report: /root/.openclaw/workspace/reports/baseline-status/baseline_status_20260527-060140.md
Allowlisted tool count: 17
Progress docs: 17
NAS workspace: not_mounted
```

## Current Next Actions

The generated report lists the next best actions as:

```text
1. Provide TS-264C share/account details so A-003 can be mounted and the NAS-dependent items can be retested.
2. Keep A-010 timer running until 7 days of stability samples exist.
3. Decide service policy for NFS/RPC, x11vnc, and iiod from the B-010 report.
4. Decide whether B-003 should stay metadata-only or add semantic vision captioning.
```
