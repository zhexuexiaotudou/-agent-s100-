# Baseline Progress: ROS Bag Session

Date: 2026-05-27

## Status

| Item | Status | Evidence |
| --- | --- | --- |
| A-009 local start/status/stop self-test | verified path | `rosbag_session_probe` starts, checks, and stops a bounded `ros2 bag record`. |
| A-009 OpenClaw trigger | verified path | `s100p_run_probe` triggered `rosbag_session_probe` and returned the status fields. |
| A-009 NAS-backed session | pending | `/mnt/nas/openclaw` is not mounted yet. |
| A-009 arbitrary long-running session | pending | The first version is a safe self-test, not an open-ended recorder. |

## Implementation

ROS bag session self-tests are generated by:

```text
scripts/probes/rosbag_session_probe.sh
```

The allowlist runner exposes it as:

```bash
scripts/run_allowlisted_tool.sh rosbag_session_probe /root/.openclaw/workspace/robot_datasets /root/.openclaw/workspace/logs/probes
```

## Runner Evidence

Runner report:

```text
/root/.openclaw/workspace/logs/probes/rosbag_session_20260527-051843.md
```

Observed fields:

```text
session_id: rosbag_session_20260527-051843
topics_requested: /rosout /parameter_events
start_status: started
status_after_start: running
stop_status: sent_sigint
record_exit: 0
metadata_exists: yes
dataset_card: /root/.openclaw/workspace/robot_datasets/rosbag_session_20260527-051843/DATASET_CARD.md
verdict: ok
```

Bag files:

```text
DATASET_CARD.md
metadata.yaml
rosbag_session_20260527-051843_0.db3
```

## OpenClaw Plugin Evidence

The OpenClaw agent used the real `s100p_run_probe` tool:

```text
runId: 0256a6af-2384-4456-bc4e-cb3a244761f2
status: ok
tool_id: rosbag_session_probe
report: /root/.openclaw/workspace/logs/probes/rosbag_session_20260527-052005.md
```

Returned status fields:

```text
start_status: started
status_after_start: running
stop_status: sent_sigint
metadata_exists: yes
verdict: ok
```

## Current A-009 Verdict

A-009 now has a verified local fallback for both bounded snapshots and start/status/stop self-tests.

It is not fully complete until NAS-backed output is validated and a policy is chosen for longer named capture sessions.
