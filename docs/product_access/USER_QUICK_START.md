# 用户快速开始

1. 将手机与地瓜 AI-NAS 接入同一 Wi-Fi。
2. 扫描访问卡或打开 `http://digua.local`；打不开时使用卡片上的备用 IP。
3. 首次使用打开 `/setup`，输入一次性认领码并创建管理员；以后直接登录。
4. 文件、搜索、问答与报告沿用现有工作台。受控写操作仍会要求预览和确认。
5. 外地访问必须由管理员先配置 Tailscale 或 Cloudflare Access；不要自行做路由器端口转发。
6. 页面显示 NAS 未连接或远程入口不可用时，LAN 页面仍可用于查看状态；联系管理员，不要反复重置设备。
