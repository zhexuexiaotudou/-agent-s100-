# Teacher Demo Path - 2026-06-09

## Decision

The current work path is changed to demo-first:

1. Finish the S100P OpenClaw entry demo.
2. Finish the AI NAS movie-sort demo.
3. After both demos are recordable, return to the Dream 7B S100 BPU route.

Robot capability, ROS2, and rosbag work are out of scope for this teacher demo path.

## Teacher Requirements

- Show that OpenClaw can run on S100P as the entry point.
- Show that the PC side does not need high-privilege operations for the demo.
- Show that persistence uses NAS storage instead of unsafe PC read/write.
- Show an AI NAS style workflow where OpenClaw on S100P organizes movies by type on NAS.
- Record the demos as videos after the two paths are runnable.
- For Dream 7B, first run an official S100 LLM quantization-to-deployment sample flow, then try replacing the official model with Dream 7B. If the Dream path blocks, keep the blocker evidence and use an official model with a complete quantization/deployment example for the demo fallback.

## Demo 1: S100P OpenClaw Entry

Runbook:

```text
docs/openclaw_entry_demo_runbook.md
```

Allowlisted command:

```bash
scripts/run_allowlisted_tool.sh openclaw_entry_demo_probe
```

Default report root:

```text
/mnt/nas/openclaw/reports/teacher-demos/openclaw-entry
```

Evidence files generated by the probe:

```text
openclaw_entry_demo.md
openclaw_entry_demo.json
captures/
```

Acceptance:

- `openclaw_entry_demo.json` has `verdict: ok_openclaw_entry_demo_probe`.
- `captures/openclaw_gateway_status.txt` or `captures/openclaw_gateway_active.txt` records the S100P `openclaw-gateway` status.
- `captures/port_18789.txt` records whether the gateway listener is present.
- The report path is under `/mnt/nas/openclaw/reports/teacher-demos/openclaw-entry` when NAS is mounted and writable.
- The demo procedure does not require elevated Windows tools or PC-side workspace writes.

## Demo 2: AI NAS Movie Sort

Runbook:

```text
docs/ai_nas_movie_sort_demo_runbook.md
```

Allowlisted command:

```bash
scripts/run_allowlisted_tool.sh ai_nas_movie_sort_demo_probe
```

Default demo root:

```text
/mnt/nas/openclaw/demo/ai-nas-movie-sort
```

Default report root:

```text
/mnt/nas/openclaw/reports/teacher-demos/ai-nas-movie-sort
```

Evidence files generated by the probe:

```text
movie_sort_demo.md
movie_sort_demo.json
/mnt/nas/openclaw/demo/ai-nas-movie-sort/library/<type>/
```

Acceptance:

- `movie_sort_demo.json` has `verdict: ok_ai_nas_movie_sort_demo_probe`.
- Movie-like demo files in `inbox/` are copied into `library/<type>/`.
- Original files in `inbox/` are preserved.
- All writes stay inside `/mnt/nas/openclaw/demo/ai-nas-movie-sort` and `/mnt/nas/openclaw/reports/teacher-demos/ai-nas-movie-sort`.
- The report states `classification_engine: deterministic_filename_metadata_rules`; this demo does not claim external model inference.

## Dream 7B Follow-Up Path

Dream 7B remains the requested model, but it is now after the two demos.

Next Dream sequence:

1. Identify the exact official S100 LLM sample that includes quantization and deployment.
2. Run the official sample end to end and preserve exact command, config, output, and error evidence.
3. Replace the official model with Dream 7B only after the official path is understood.
4. If Dream 7B blocks, record the exact blocker and hand it off for external help.
5. If the blocker cannot be solved in time, use an official model with complete quantization/deployment evidence as the fallback demo model.

Current Dream 7B status remains documented in:

```text
docs/dream7b_s100p_bpu_handoff_2026-06-09.md
docs/baseline_progress_2026-06-03_dream7b_segmented_bpu_hbm.md
```

## Documentation Check

After changing demo scripts, allowlist, README, or project decisions, run:

```bash
bash scripts/probes/project_docs_consistency_probe.sh /tmp/project_docs_consistency
```
