# Baseline Progress: Security Audit

Date: 2026-05-27

## Status

| Item | Status | Evidence |
| --- | --- | --- |
| B-010 local security audit | verified path | `security_audit_probe` writes a redacted Markdown audit from local board state. |
| B-010 OpenClaw trigger | verified path | `s100p_run_probe` triggered `security_audit_probe` and returned the verdict matrix. |
| B-010 NAS-backed audit | pending | `/mnt/nas/openclaw` is not mounted yet. |

## Implementation

Security audits are generated by:

```text
scripts/probes/security_audit_probe.sh
```

The allowlist runner exposes it as:

```bash
scripts/run_allowlisted_tool.sh security_audit_probe /root/.openclaw/workspace/logs/probes
```

## Runner Evidence

Runner report:

```text
/root/.openclaw/workspace/logs/probes/security_audit_20260527-045500.md
/root/.openclaw/workspace/logs/probes/security_audit_20260527-050115.md
```

Observed verdict rows:

```text
OpenClaw config validation: pass
Gateway exposure: pass, loopback only
Tavily plugin: pass
S100P allowlisted plugin: pass
Non-loopback listeners: warn, 19 non-loopback listeners
NAS workspace mount: warn, not_mounted
Workspace secret scan: pass, no secret-like text found in scanned workspace files
```

Listener review categories in the latest runner report:

```text
nfs-rpc: rpcbind, rpc.statd, rpc.mountd, nfs, nlockmgr/kernel RPC listeners
admin: sshd on port 22
remote-desktop: x11vnc on port 5900
hardware-daemon: iiod on port 30431
```

## OpenClaw Plugin Evidence

The OpenClaw agent used the real `s100p_run_probe` tool:

```text
runId: c9778552-a4d7-494e-b325-e3eab7906086
runId: e08be89a-f0ee-43d2-8b4a-cf8ab92a7ff3
status: ok
tool_id: security_audit_probe
report: /root/.openclaw/workspace/logs/probes/security_audit_20260527-045534.md
report: /root/.openclaw/workspace/logs/probes/security_audit_20260527-050149.md
```

Returned verdict rows:

```text
OpenClaw config validation: pass
Gateway exposure: pass
Tavily plugin: pass
S100P allowlisted plugin: pass
Non-loopback listeners: warn
NAS workspace mount: warn
Workspace secret scan: pass
```

## Service Policy Evidence

Runner report:

```text
/root/.openclaw/workspace/logs/probes/service_policy_20260527-050451.md
```

OpenClaw agent evidence:

```text
runId: e71ee679-f023-4e7f-97b5-3a2fec8e9c58
status: ok
tool_id: service_policy_probe
report: /root/.openclaw/workspace/logs/probes/service_policy_20260527-050558.md
```

Returned policy matrix:

```text
OpenClaw Gateway: loopback, keep current binding
SSH: present, keep for trusted management
NFS/RPC server stack: present, disable after confirming S100P is not exporting NFS shares
x11vnc: present, disable after confirming RDK Studio terminal/file access is enough
iiod: present, keep if IIO tooling is needed; otherwise disable or firewall
```

## Current B-010 Verdict

B-010 is verified for local workspace fallback through both the runner and the narrow OpenClaw plugin.

The non-loopback listener list is now classified and a read-only service policy plan exists, but B-010 is not fully complete until NAS-backed report output is validated and the final service policy decides whether to keep or close NFS/RPC, x11vnc, and iiod.
