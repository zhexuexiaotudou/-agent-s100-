# Baseline Progress: Experiment Report

Date: 2026-05-27

## Status

| Item | Status | Evidence |
| --- | --- | --- |
| B-007 local report generation | verified path | `experiment_report_probe` writes a Markdown report from existing local workspace artifacts. |
| B-007 OpenClaw trigger | verified path | `s100p_run_probe` triggered `experiment_report_probe` and returned a report path plus summary counts. |
| B-007 NAS-backed report | pending | `/mnt/nas/openclaw` is not mounted yet. |

## Implementation

Experiment reports are generated by:

```text
scripts/probes/experiment_report_probe.sh
```

The allowlist runner exposes it as:

```bash
scripts/run_allowlisted_tool.sh experiment_report_probe /root/.openclaw/workspace/reports/experiments
```

## Runner Evidence

Runner report:

```text
/root/.openclaw/workspace/reports/experiments/experiment_report_20260527-044531.md
```

Runner exit code:

```text
exit:0
```

## OpenClaw Plugin Evidence

The OpenClaw agent used the real `s100p_run_probe` tool:

```text
runId: 274ef269-05fd-406c-a0b6-64e756b77530
status: ok
tool_id: experiment_report_probe
report: /root/.openclaw/workspace/reports/experiments/experiment_report_20260527-044552.md
```

Returned summary counts:

```text
Probe reports: 15
Experiment reports: 6
Browser smoke screenshots: 3
Document indexes: 2
ROS bag datasets: 5
Dataset cards: 2
```

## Current B-007 Verdict

B-007 is verified for local workspace fallback through both the runner and the narrow OpenClaw plugin.

It is not NAS-backed until A-003 mounts `/mnt/nas/openclaw` and the same report writes under:

```text
/mnt/nas/openclaw/reports/experiments
```
