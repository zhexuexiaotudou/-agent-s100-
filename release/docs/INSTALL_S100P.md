# Install S100P

Run `bash release/install/install_s100p.sh --dry-run` first, then apply the
reviewed command with NAS protocol, host, share, mount point, Personal root, and
install root. The installer uses a venv and does not modify system Python.

