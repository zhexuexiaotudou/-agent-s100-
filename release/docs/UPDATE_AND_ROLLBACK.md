# Update And Rollback

Use `release/install/upgrade_s100p.sh --dry-run` before applying an upgrade.
The rollback path restores the previous install root and leaves NAS Personal
data untouched. Use `release/install/uninstall_s100p.sh --dry-run` before
removing services.

